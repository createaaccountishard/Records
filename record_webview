使用android webview 加载网页

1 对文件的处理    <input type="file" ....>

在chroml的客户端类中实现 file的相方法.需要注意的是: 在按下取消或者回退键的时候,不能二次吊起file的方法
解决: 思路应该是通过设置回调为空,结束本次的任务,并重新激活下次的连接.

/**
     * 防止点击dialog的取消按钮之后，就不再次响应点击事件了
     * 在dialog取消的时候调用,setoncanclelisterner()或者是其他需要的地方
*/
    public void cancelCallback() {
        if (mUploadCallbackAboveL != null) {
            mUploadCallbackAboveL.onReceiveValue(null);
        }
        if (mUploadMessage != null) {
            mUploadMessage.onReceiveValue(null);
        }
    }
