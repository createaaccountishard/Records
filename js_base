1 在页面上输出内容
document.write(**);   
内容可以说是字符串,也可以是变量,还可以是符合html的内容,document.write("<h1>这是一个标题</h1>")
只能在加载页面的时候使用,否则会覆盖body中的内容.也就是说不要在页面加载完成后使用的方法中使用这个方法.

2 在标签内输入内容
document.findElementById(**).innerHTML=**;本质上是在标签中插入内容   <p>**(在这个位置插入的内容)</p>
本身会复写已经存在的内容

3 引入其他文件中的js内容
<script type="text/javascript" src="./****"></script>  
type 可以省略 ,src可以执行本地文件,也可以是网址.
脚本可被放置在 HTML 页面的 <body> 和 <head> 部分中,标签的位置随意,浏览器再加载的时候会将其提前加载

