
主要分成两部分，

1   2  是对类自身的要求

3...6  是对类之间的要求

1 单一职责
  
   每个类的职责是单一的，只负责相关地高的部分。

2 开闭原则
   
   类应当对扩展是开放的，对修改是关闭的。这个应该是对对稳定的版本来说的。

3 替换原则

   使用父类的声明，指向子类的实例。

4 依赖倒置原则
 
   传递的参数应当是抽象的，不依赖具体的实现。

5 接口隔离原则

  类之间的依赖关系应当建立在最小的接口上。

6 迪米特原则

  一个对象应该对其他对象有最少的了解。只需要知道有什么功能，不需要知道实现。